<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.AdminMapper">

	<insert id="insertpro">
		<selectKey resultType="int" keyProperty="productId"
			order="AFTER">

			SELECT LAST_INSERT_ID()

		</selectKey>
		insert into product(product_id, product_name, product_price,
		product_size, product_color, product_inventory)
		values(#{productId},#{productName},#{productPrice},#{productSize},#{productColor},#{productInventory})

	</insert>
	<!-- 이미지 등록 -->
	<insert id="imageEnroll">

		insert into product_img(product_id, file_name,
		upload_path, uuid) values
		(#{productId}, #{fileName}, #{uploadPath},
		#{uuid})

	</insert>

	<!-- 상품목록 -->
	<select id="selectproductList"
		resultType="com.shop.model.Product">
		select
		p.product_id,p.product_name,p.product_price,p.product_size,p.product_color,
		p.product_inventory,i.file_name,i.upload_path,i.uuid from product p
		join product_img i on p.product_id=i.product_id
	</select>
	<!-- 이미지목록 -->
	<select id="selectimgList"
		resultType="com.shop.model.AttachImageVO">
		select*from product_img
	</select>

	<delete id="deleterProdect">
		delete from product where product_id = #{productId}
	</delete>

	<delete id="userDelete">
		delete from user where user_id = #{userId}
	</delete>

	<delete id="deleterProdectImg">
		delete from product_img where product_id =
		#{productId}
	</delete>


	<!-- 상품 수정 조회 페이지 -->

	<select id="goodsUpdateId" resultType="com.shop.model.Product">

		select
		p.product_id,p.product_name,p.product_price,p.product_size,p.product_color,
		p.product_inventory,i.file_name,i.upload_path,i.uuid from product p
		join product_img i on p.product_id=i.product_id where p.product_id =
		#{productId}

	</select>

	<update id="goodsUpdateProduct">
		update product set product_name=#{productName},
		product_price=#{productPrice},product_size=#{productSize},product_color=#{productColor},
		product_inventory=#{productInventory} where product_id=#{productId};
	</update>
	<update id="goodsUpdateProductImg">
		update product_img set
		file_name=#{fileName},upload_path=#{uploadPath},uuid=#{uuid} where
		product_id=#{productId}
	</update>



</mapper>