<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.shop.mapper.AdminMapper">
  
  <insert id="insertpro">
  	
  		insert into product(product_id, product_name, product_img, product_price, product_size, product_color, product_inventory)
		values(#{productId},#{productName}, #{productImg},#{productPrice},#{productSize},#{productColor},#{productInventory})

  	</insert>
  
  
  </mapper>