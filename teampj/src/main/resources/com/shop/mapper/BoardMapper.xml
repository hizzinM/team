<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.BoardMapper">

	<!-- 공지 등록 -->
	<insert id="enroll">
		insert into notice(notice_title, notice_content,
		user_id) values
		(#{noticeTitle}, #{noticeContent},
		#{userId})
	</insert>

	<!-- 공지 목록 -->
	<select id="getList" resultType="com.shop.model.NoticeVO">
		select * from notice
	</select>

	<!-- 공지 조회 -->
	<select id="getPage" resultType="com.shop.model.NoticeVO">
		select * from notice where bno =
		#{bno}
	</select>

	<!-- 공지 수정 -->
	<update id="modify">
		update notice set notice_title = #{noticeTitle},
		notice_content =
		#{noticeContent}, notice_bold=#{noticeBold} where bno
		= #{bno}
	</update>

	<!-- 공지 삭제 -->
	<delete id="delete">
		delete from notice where bno = #{bno}
	</delete>

	<!-- 게시물 목록(페이징) -->
	<select id="getListPaging" resultType="com.shop.model.NoticeVO">
		select * from (
		select *
		from notice order by bno desc) as Page
		limit #{skip},#{amount}
	</select>

	<!-- 게시물 총 개숫 -->
	<select id="getTotal" resultType="int">
		select count(*) from notice
	</select>
 
	<!-- QNA 등록 -->
	<insert id="enrollQNA">
		insert into
		qna(qna_title,qna_category,user_id,qna_contents ) values
		(#{qnaTitle},
		#{qnaCategory},#{userId},#{qnaContents} )
	</insert>

	<!-- QNA 목록 -->
	<select id="getQNAList" resultType="com.shop.model.QnaVO">
		select * from qna
	</select>

	<!-- QNA 조회 -->
	<select id="getQNAPage" resultType="com.shop.model.QnaVO">
		select * from qna where
		qna_id =
		#{qnaId}
	</select>

	<!-- QNA 수정 -->
	<update id="modifyQNA">
		update qna set qna_title = #{qnaTitle},
		qna_category =
		#{qnaCategory}, qna_contents=#{qnaContents} where qna_id
		= #{qnaId}
	</update>

	<!-- QNA 삭제 -->
	<delete id="deleteQNA">
		delete from qna where qna_id = #{qnaId}
	</delete>

	<!-- QNA 목록(페이징) -->
	<select id="getQnaListPaging" resultType="com.shop.model.QnaVO">
		select * from (
		select *
		from qna order by qna_id desc) as Page2
		limit #{skip},#{amount}
	</select>

	<!-- QNA 총 개숫 -->
	<select id="getQnaTotal" resultType="int">
		select count(*) from qna
	</select>


</mapper>